export type AuthFormData = {
  username: string;
  password: string;
};

export type LoginData = {
  token: string;
  username: string;
};

export type ProductsType = {
  _id: string;
  title: string;
  price: number;
  description: string;
};
