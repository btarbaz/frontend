import React, { Fragment } from 'react';
import NavBar from '../components/NavBar';

const Home: React.FC = () => {
  return (
    <Fragment>
      <NavBar />
    </Fragment>
  );
};

export default Home;
